server:
  port: 8090

zuul:
  ignoredServices: '*'
  host:
    connect-timeout-millis: 20000
    socket-timeout-millis: 20000
    routes:
      heroes-service:
        path: /heroes/**
        url: heroes-service
        stripPrefix: false
        sensitiveHeaders:
